type Query {
    getUserById (id: ID!): User
    getUsers: [User]
}

type Mutation {
    createUser(username: String!, age: Int!, email:String): User
    createProduct(name: Int!, count)
}

type User {
    id: ID!
    username: String!
    age: Int!
    email:String
}
